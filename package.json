{
  "name": "visualai-v5",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "@vercel/node": "^1.14.0"
  },
  "dependencies": {
    "openai": "^4.19.0"
  }
}
